<h1>My tasks</h1>

<% @tasks.each do |task| %>
    <% if task.completed %>
      <p>☑<%= link_to task.title, task_path(task.id) %> <%= link_to '🗑', task_path(task.id), method: :delete, data: { confirm: "Delete #{task.title}?" }%></p>
    <% else %>
      <p>☐<%= link_to task.title, task_path(task.id) %> <%= link_to '🗑', task_path(task.id), method: :delete, data: { confirm: "Delete #{task.title}?" }%></p>
    <% end %>
<% end %>

<%= link_to 'Add a new task', new_task_path  %>
